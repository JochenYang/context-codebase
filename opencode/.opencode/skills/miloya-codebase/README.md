# Replace This Placeholder

Put the real `miloya-codebase` skill files in this directory if you want to
ship the skill with the target OpenCode project.

Expected minimum layout:

```text
.opencode/skills/miloya-codebase/
  SKILL.md
  scripts/
    generate.py
```

If the skill is installed globally instead, you can remove this placeholder
directory and keep only the command files plus `opencode.json` permission
config.
