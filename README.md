# context-codebase
context-codebase is a project context engine that generates reusable snapshots for fast repository understanding and task-focused code retrieval. Works as a skill in any AI coding agent — no model downloads, no vector computation, no external dependencies beyond Python stdlib + SQLite.
